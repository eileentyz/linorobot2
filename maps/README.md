# Project maps

| Map | Resolution | Origin (x, y, yaw) | Status |
| --- | --- | --- | --- |
| `moretea_test_map` | 0.05 m/pixel | (0.636, -0.784, 0) | Copied from the existing local workspace; capture environment and navigation validation not yet documented. |

`moretea_test_map.yaml` and `moretea_test_map.pgm` must stay together. The YAML uses a relative image path so the pair remains portable.

Save new project maps here, outside the ignored upstream clone. Use descriptive names and record the matching simulation world or physical environment, base configuration, date, and validation result.
